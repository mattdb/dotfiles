- clean: ['~']

- link:
    ~/.bash_profile: bash_profile
    ~/.gitconfig: gitconfig
    # ~/.ssh/config:
    #   path: ssh/config
    #   create: true
    ~/.zshrc: zshrc

- brewfile:
    # This accepts the same options as `brew bundle` command:
    file: Brewfile
